
<h1>{{ page.name() }}</h1>

<p><code>{{ servant.format().path('formatted/path', 'domain') }}</code></p>

<ul>
{% for id in page.siblings() %}
	<li><a href="{{ id }}">{{ servant.format().name(id) }}</a></li>
{% endfor %}
</ul>
