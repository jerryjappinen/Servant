
# Theme

Themes consist of *stylesheets* and *scripts*. `CSS`, `LESS`, `SCSS` are supported for styles and `JS` for scripts. All theme files and assets are packaged into one directory where they can be organized freely. Servant will load all supported stylesheets and scripts automatically.
