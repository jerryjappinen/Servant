
# Look

Your site's look is defined in *stylesheets* and *scripts*. `CSS`, `LESS`, `SCSS` are supported for styles and `JS` for scripts. All style files are packaged into one directory where they can be organized freely. Servant will load all supported stylesheets and scripts automatically.
